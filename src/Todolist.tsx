import React from 'react';

export type TaskType = {
    id: string
    title: string
    isDone: boolean
}

const Todolist = () => {
    return (
        <div>
            
        </div>
    );
};

export default Todolist;